[tool.poetry]
name = "fern-fix6"
version = "0.0.5"
description = ""
readme = "README.md"
authors = []
packages = [
    { include = "fix6", from = "src"}
]

[tool.poetry.dependencies]
python = "^3.7"
pydantic = "^1.9.2"
httpx = "0.23.3"

[tool.poetry.dev-dependencies]
mypy = "0.971"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
