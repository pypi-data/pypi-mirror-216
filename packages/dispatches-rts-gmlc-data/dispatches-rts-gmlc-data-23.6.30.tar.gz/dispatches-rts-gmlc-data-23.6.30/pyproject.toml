[build-system]
requires = [
    "setuptools >= 64",
    "setuptools_scm >= 7",
]
build-backend = "setuptools.build_meta"

[project]
name = "dispatches-rts-gmlc-data" 
readme = "README.md"
dynamic = ["version"]

[tool.setuptools_scm]