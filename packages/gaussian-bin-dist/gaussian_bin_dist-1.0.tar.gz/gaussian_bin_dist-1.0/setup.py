from setuptools import setup

setup(name='gaussian_bin_dist',
      version='1.0',
      description='Gaussian distributions',
      packages=['gaussian_bin_dist'],
      author='Parth Mehta',
      zip_safe=False)
