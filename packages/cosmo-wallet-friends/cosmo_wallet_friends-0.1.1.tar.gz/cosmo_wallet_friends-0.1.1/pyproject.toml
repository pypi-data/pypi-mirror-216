[tool.poetry]
name = "cosmo-wallet-friends"
version = "0.1.1"
description = ""
authors = ["Bruno-Felix <balvesfelix@gmail.com>"]
readme = "README.md"
packages = [{include = "cosmo_wallet_friends"}]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
