[tool.poetry]
name = "promptSDK"
version = "0.1.2"
description = "An SDK for managing prompts via github"
authors = ["otto zastrow <otto@midpage.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"
configparser = "^5.3.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project.urls]
"Homepage" = "https://github.com/midpage-ai/promptSDK"