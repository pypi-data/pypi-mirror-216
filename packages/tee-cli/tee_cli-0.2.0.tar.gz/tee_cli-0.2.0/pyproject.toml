[tool.poetry]
name = "tee-cli"
version = "0.2.0"
description = ""
authors = ["Bohdan Vanieiev <warchantua@gmail.com>"]
readme = "README.md"
packages = [{include = "tee_cli", from="src"}]

[tool.poetry.scripts]
tee-cli = "tee_cli.__main__:main"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
