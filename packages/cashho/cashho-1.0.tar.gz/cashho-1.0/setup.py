from setuptools import setup

setup(
    name='cashho',
    version='1.0',
    packages=['src'],
    install_requires=[
        'Django>=3.2',
        # add other dependencies here
    ],
)
